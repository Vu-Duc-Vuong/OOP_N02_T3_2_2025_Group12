SPRING_DATASOURCE_PASSWORD=your_mysql_password_here

# Cách dùng:
# 1) cp .env.example .env
# 2) Sửa giá trị mật khẩu thật
# 3) source .env (hoặc: set -a; source .env; set +a)
# 4) mvn spring-boot:run -Dspring-boot.run.profiles=mysql
